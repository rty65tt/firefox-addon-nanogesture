# firefox-addon-nanogesture


## NanoGestures

This simple extension adds basic mouse gestures for Go Up/Down(Up/Down), Previos/Next Tab(Left/Right), Close tab(DownRight), Minimize Window(DownLeft), Reload Page(UpDown).
